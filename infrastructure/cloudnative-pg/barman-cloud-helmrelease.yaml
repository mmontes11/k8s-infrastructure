apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: barman-cloud
spec:
  chartRef:
    kind: OCIRepository
    name: barman-cloud
  dependsOn:
  - name: cloudnative-pg
  interval: 1h0m0s
  install:
    crds: CreateReplace
  upgrade:
    crds: CreateReplace
  healthChecks:
    runs:
    - kind: Deployment
      name: barman-cloud
