apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: volsync
spec:
  chart:
    spec:
      chart: volsync
      sourceRef:
        kind: HelmRepository
        name: volsync
      version: "v0.10.0"
  interval: 1h0m0s
  values:
    replicaCount: 1

    image:
      repository: ghcr.io/onedr0p/volsync
    rclone:
      repository: ghcr.io/onedr0p/volsync
    restic:
      repository: ghcr.io/onedr0p/volsync
    rsync:
      repository: ghcr.io/onedr0p/volsync
    rsync-tls:
      repository: ghcr.io/onedr0p/volsync
    syncthing:
      repository: ghcr.io/onedr0p/volsync

    manageCRDs: true

    metrics:
      disableAuth: true