apiVersion: v1
kind: Service
metadata:
  name: redis-operator
  labels:
    app.kubernetes.io/name: redis-operator
    app.kubernetes.io/component: operator
spec:
  ports:
    - name: metrics
      port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app.kubernetes.io/name: redis-operator
