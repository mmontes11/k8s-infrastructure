apiVersion: v1
kind: Service
metadata:
  name: redis-operator
  labels:
    name: redis-operator  
spec:
  ports:
    - name: metrics
      port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    name: redis-operator  
