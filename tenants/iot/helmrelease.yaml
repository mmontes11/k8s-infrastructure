apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: iot
spec:
  releaseName: iot
  targetNamespace: iot
  chart:
    spec:
      chart: ./charts/tenant
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
  interval: 5m
  values:
    repo:
      url: ssh://git@github.com/mmontes11/k8s-iot
      branch: kubernetes-1.25
      path: ./clusters/production
      secretName: flux-system
